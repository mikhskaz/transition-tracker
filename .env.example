# Spotify Web API — optional. App works fully without these (manual entry).
# Create an app at https://developer.spotify.com and add the redirect URI below.
VITE_SPOTIFY_CLIENT_ID=...
VITE_SPOTIFY_REDIRECT_URI=...
